% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan-models.R
\name{.get_stan_model}
\alias{.get_stan_model}
\title{Get a compiled cmdstanr model, compiling (and caching) on first use}
\usage{
.get_stan_model(model_name, quiet = TRUE, force_recompile = FALSE)
}
\arguments{
\item{model_name}{One of "model_betadir", "model_pureNMF", "model_test"}

\item{quiet}{Logical, passed to cmdstanr's compile() to suppress
compiler output (default TRUE)}

\item{force_recompile}{Logical, force recompilation even if a cached
executable already exists (default FALSE)}
}
\value{
A cmdstanr::CmdStanModel object
}
\description{
This is the single entry point every fitting function should call instead
of hardcoding "stan_files/model_xxx.exe" / ".stan" paths. It:
\enumerate{
\item Finds the shipped .stan source via system.file()
\item Checks the user's cache dir for an up-to-date compiled executable
\item Compiles (once) if missing or stale, writing the exe into the cache
\item Returns a ready-to-use CmdStanModel object
}
}
\keyword{internal}
