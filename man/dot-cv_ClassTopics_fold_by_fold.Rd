% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClassTopics.R
\name{.cv_ClassTopics_fold_by_fold}
\alias{.cv_ClassTopics_fold_by_fold}
\title{K-Fold Cross-Validation for ClassTopics (For unbiased estimation)}
\usage{
.cv_ClassTopics_fold_by_fold(
  gene_counts,
  response,
  folds,
  fold,
  K_topics = 3,
  sigma_eta = 0.5,
  betadir = TRUE,
  alpha_beta = 0.05,
  seed = 123,
  verbose_per_fold = TRUE,
  iter_warmup = 1000,
  iter_sampling = 1000,
  n_iter_EM = 4,
  n_iter_EM_test = 4,
  test_stan = TRUE,
  lambda_cat = 10000,
  lambda_ridge = 0,
  ...
)
}
\arguments{
\item{gene_counts}{gene count matrix}

\item{response}{character or factor vector on the class labels}

\item{folds}{list whose elements are train-test partitions}

\item{fold}{index of the fold to be fitted a ClassTopics model}

\item{K_topics}{number of topics to be modeled (default = \code{3})}

\item{sigma_eta}{scalar prior sd of the coefficients of the eta matrix. If
NULL (default), it is internally defined as
\eqn{2/\sqrt{lambda_cat}}}

\item{betadir}{logical, controls whether beta is estimated with Dirichlet
prior (default = \code{TRUE}) or, if \code{FALSE}, with Gamma prior}

\item{alpha_beta}{numeric hyperparameter of the symmetric Dirichlet
distribution if \code{betadir} = TRUE] (default = \code{0.05})}

\item{seed}{numeric seed for reproducibility (default = \code{123})}

\item{verbose_per_fold}{Logical, print details for each fold (default: \code{TRUE})}

\item{iter_warmup}{number of warm-up iterations for each chain
(default = \code{1000})}

\item{iter_sampling}{number of sampling iterations for each chain
(default = \code{1000})}

\item{n_iter_EM}{number of iterations of the EM pre-processing (default = \code{10})}

\item{n_iter_EM_test}{number of EM iterations used when fitting theta on
this fold's held-out (test) data (default = \code{4})}

\item{test_stan}{logical, if \code{TRUE} (default) test-fold topic
proportions are fit via Stan (see
\code{\link[=predict_ClassTopics_stan]{predict_ClassTopics_stan()}}); if \code{FALSE}, via EM
only (see \code{\link[=predict_ClassTopics_EM]{predict_ClassTopics_EM()}})}

\item{lambda_cat}{numeric value that represents a weight for the supervised
component in model estimation (default = \code{1})}

\item{lambda_ridge}{numeric ridge-penalty weight applied to the
regression coefficients \code{eta} (default = \code{0},
i.e. no ridge penalty)}

\item{...}{Additional arguments passed to \code{\link[=ClassTopics]{ClassTopics()}} and \code{\link[=predict_ClassTopics_stan]{predict_ClassTopics_stan()}}}
}
\value{
List with accuracy estimates and predictions for one of the folds
}
\description{
K-Fold Cross-Validation for ClassTopics (For unbiased estimation)
}
\details{
This performs k-fold cross-validation:
\itemize{
\item Each patient appears in test set exactly ONCE
\item Training sets are DISJOINT (no overlap)
\item Accuracy estimate is UNBIASED
}

Returns:
\itemize{
\item cv_accuracy: Unbiased estimate of model performance
\item final_model: Model trained on ALL data (for interpretation)
}
}
\keyword{internal}
